#ifndef SQUARE_H
#define SQUARE_H
namespace square {
struct parameters {
 double X,Y;
};
double compute_perimeter(parameters *ptr_x, parameters *ptr_y, parameters *ptr_z, parameters *ptr_k);
}
#endif // SQUARE_H
